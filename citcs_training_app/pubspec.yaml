name: citcs_training_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.15.0
  firebase_auth: ^4.17.0
  cloud_firestore: ^4.17.5
  firebase_auth_web: ^5.8.13
  firebase_storage: ^11.0.17  # Added Firebase Storage for video uploads
  cupertino_icons: ^1.0.8
  google_fonts: ^5.0.0
  provider: ^6.0.5
  image_picker: ^1.0.0+5  # Added Image Picker for selecting videos
  file_picker: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
    - assets/images/bg_image.png
